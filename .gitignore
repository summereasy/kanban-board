# bun.lock is required for CI (force-add: git add -f bun.lock)
node_modules/
dist/
*.log
*.tsbuildinfo
.DS_Store
.env
.env.local

# persisted runtime state — auto-created on first run
server/data/db.json
server/data/board.json
server/data/*.migrated
